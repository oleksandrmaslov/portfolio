{
  "name": "landing-runtime-build",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic compiler for the static landing runtime.",
  "scripts": {
    "build": "node build.cjs",
    "check": "node build.cjs --check"
  },
  "devDependencies": {
    "@babel/standalone": "7.29.0"
  }
}
